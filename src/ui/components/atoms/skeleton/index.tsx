import React from 'react';

const Skeleton = () => {
	return (
		<div className="animate-pulse w-full h-full">
			<div className="w-full h-full bg-light-gray-2" />
		</div>
	);
};

export default Skeleton;
